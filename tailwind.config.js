/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  darkMode: "class", //enabling the dark mode variant in tailwind css otherwise it will not work accordingly
  theme: {
    extend: {},
  },
  plugins: [],
};
